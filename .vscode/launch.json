{
  "version": "0.2.0",
  "configurations": [{
    "name": "Ktory VS Code Extension",
    "type": "extensionHost",
    "request": "launch",
    "args": ["--extensionDevelopmentPath=${workspaceFolder}/src/Ktory.VSCode"],
    "preLaunchTask": "Build Ktory VS Code extension"
  }]
}
